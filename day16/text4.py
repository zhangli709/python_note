# Subversion 集中式（多数）
# Git  分布式，kgjhgkj


## 发展史
    # 1.CVS -- Voncurrent Version System
    # 2.VSS - Visual Source Safe  锁模式
2000
    # Subversion 合并模式 必须有中央服务器
2005
    # Git - L  合并模式，只是在提交时需要使用中央服务器
## Git
    # 1.git --version 检查是否安装成功
    # 2.创建目录 mkdir
    # 3. 进入此目录下。
    # 4.初始化仓库 git init
    # 5.repository --仓库
    # 6.git add .(表示所有)， 可以填具体的文件。 表示加载了到零食仓库中，还没有提交。
    # 7.git remove + 文件名， 可以删除它。
    # global 设置，在第一次使用时需要填用户名。
    # 8.git commit -m '提交原因'提交。
    # 9.git log 查看历史版本
    # 10.git reset + 版本号  回档
    # git remote add origin https://git.coding.net/zhangli709/sdgdg.git
    # git push -u origin master
