# # 函数
#
# 名字
# 参数/ 默认值/ 可变参数/ 关键字参数/ 命名关键字参数/
# *args  **kwargs *
# 返回值，对象，字符串，元组
#
# #嵌套函数,
# 高阶函数  Lambda函数（匿名函数），  闭包， 偏函数，柯里化
# 作用域，局部作用域，嵌套作用域， 全局作用域， 内置作用域。
# 标识符 LEGB local enclose global build_in    (nonlocal global)
# lambda  代码极其少的时候，取消定义函数，直接使用在参数里的函数。
#
# # 类和对象
#
# 类 属性+方法
# 对象关系
#
# # 面向对象
# use a   has a  is a
# 继承， 多态， （高类聚，低耦合）
#
#
# # 七大原则
# 单一职责原则
# 开闭原则
# 依赖倒转原则
# 里氏替换原则
# 接口隔离原则
# 合成聚合复用原则
# 迪米特法则（最少知识原则）
#
# #Gof设计模式